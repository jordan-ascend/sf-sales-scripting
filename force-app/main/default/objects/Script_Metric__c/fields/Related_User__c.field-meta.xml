<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_User__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>If there is a script for a specific user, then that gets added to the metric record. There is a version of this record without any users tied to it and then copies for different users. Since a user may have specific scripts assigned to him or her leading to a different set of scripts of an opportunity.</description>
    <externalId>false</externalId>
    <label>Related User</label>
    <referenceTo>User</referenceTo>
    <relationshipName>Script_Metrics</relationshipName>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
